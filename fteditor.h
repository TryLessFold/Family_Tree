#ifndef FTEDITOR_H
#define FTEDITOR_H

#include <QMainWindow>
#include <QPushButton>
#include <QGroupBox>

class FTEditor : public QMainWindow
{
    Q_OBJECT

public:
    explicit FTEditor(QWidget *parent = 0);
    ~FTEditor();

private:

public slots:

};

#endif // FTEDITOR_H
